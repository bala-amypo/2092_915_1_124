# -----------------------
# Server Configuration
# -----------------------
server.port=9001

# -----------------------
# H2 Database Configuration
# -----------------------
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# -----------------------
# JPA / Hibernate Configuration
# -----------------------
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Avoid using reserved keywords in H2 like 'year' or 'user' by quoting identifiers
spring.jpa.hibernate.globally_quoted_identifiers=true

# -----------------------
# Swagger / OpenAPI
# -----------------------
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# -----------------------
# CORS / Security (optional hints for future)
# -----------------------
# You can configure allowed origins if needed:
# spring.web.cors.allowed-origins=http://localhost:9001
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
